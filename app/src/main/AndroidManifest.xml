<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rustedwheel.android.banktransactions">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".BTApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankTransactions">
        <activity
            android:name=".screens.SplashActivity"
            android:theme="@style/Theme.BankTransactions.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screens.home.HomeActivity"
            android:label="@string/title_activity_home" />
        <activity
            android:name=".screens.transactiondetails.TransactionDetailsActivity"
            android:label="@string/title_activity_transaction_details" />
    </application>

</manifest>